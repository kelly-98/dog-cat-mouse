git init
git status
git add
git commit

git log
git show 
git diff

working directory 
staging area
git repository
gitk

git checkout
git reset

git checkout -b <branch> (branching)
git checkout <branch>
git merge 
// merge
chuyen branch B sang A (A <---- B)
1. git checkout A (chuyen ve nhanh A)
2. git merge B
// delete
git branch -D <file> // xoa file

git reset --soft  <to_commit>
git reset --mixed <to_commit>
git reset --hard (xoa han, luu y) <to_commit>

git revert <commit> // khac voi reset ntn: khong phai commit moi nhat, 
//ma la commit da cu roi

gitignore

github
git push

git config --global credential.helper store 
^^^ not recommend (~/.git-crendentials)

git config --global credential.helper "cache --timeout=18000"

google "gnome-keyring" "git ssh"

git clone
git pull

// Pull request
1. git checkout -b <feature_branch>
2. git push origin <branch>
3. create a pull request on github
4. review code
	4.1 review code online (github)
	or 4.2 fetch branch into local to test offline (optional)
	4.3 approve the pull request
5. merge to master 





